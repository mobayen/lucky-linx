{
  "private": true,
  "scripts": {
    "build": "NITRO_PRESET=firebase nuxt build && cd .output/server && rm -rf node_modules && yarn install",
    "lint": "eslint . --ext .vue,.js,.ts --fix --ignore-path .gitignore",
    "deploy": "firebase deploy",
    "bnd": "yarn build && yarn deploy",
    "bne": "yarn build && yarn fbe",
    "dev": "nuxt dev",
    "fbe": "firebase emulators:start --import=../fb-emulators-data --export-on-exit=../fb-emulators-data",
    "generate": "nuxt generate",
    "postinstall": "nuxt prepare",
    "preview": "nuxt preview"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.4.5",
    "@vueuse/core": "^9.6.0",
    "firebase": "^9.14.0",
    "pinia": "^2.0.26",
    "validator": "^13.7.0"
  },
  "devDependencies": {
    "@nuxtjs/eslint-config-typescript": "^12.0.0",
    "@types/validator": "^13.7.10",
    "autoprefixer": "^10.4.13",
    "daisyui": "^2.43.0",
    "eslint": "^8.28.0",
    "firebase-admin": "^11.3.0",
    "firebase-functions": "^4.1.0",
    "firebase-functions-test": "^3.0.0",
    "nuxt": "3.0.0",
    "postcss": "^8.4.19",
    "tailwindcss": "^3.2.4",
    "typescript": "^4.9.3"
  }
}
